{
  "name": "agen-link-mcp",
  "version": "0.3.2",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "description": "MCP server exposing the live Unity Editor to an AI CLI (Claude Code, Antigravity, or Codex) over a localhost TCP bridge: reads console, compile errors, project info and the scene, and drives GameObjects, components, assets, play mode and the Test Runner.",
  "bin": {
    "agen-link-mcp": "build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.4.5"
  }
}
